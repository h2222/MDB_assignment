## hierarchical clutser and k-mean algorithm


## hierarchical cluster

### 1. introduction

`h_cluster.py`: implement hierarchical cluster

### 2. run
`python h_cluster.py`


<br><br><br>


## k-mean algorithm

### 1.introduction
`k_mean.py` : implement k-mean algorithm

### 2.how to run
`python k_mean.py`

### 3. result
`k-mean.png` show the result of k-mean with iris dataset, we set k=3, because we find iris data have 3 categories, and K-means looks for a fixed number (k) of clusters in a dataset. so if we want to cluster iris data into 3 categories, we set k=3


